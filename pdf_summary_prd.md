# PDF Summary Tool — Mini PRD

## Goal

Build a command-line tool named `pdf_summary.py` that extracts text from a PDF and generates a structured summary with page citations.

## Usage

```bash
python pdf_summary.py <path-to-pdf>
